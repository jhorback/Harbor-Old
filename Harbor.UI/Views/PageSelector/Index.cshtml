<section class="page centered" data-templatefor="pageSelectorView">
    <div class="page-header">
        <h1 class="float-left">Pages</h1>
        <div class="float-right">
            <button type="button" data-event="close">Cancel</button>
        </div>
    </div>
    <div class="page-body">
        <div class="page-content">
            <form data-event="submit: formSubmit">
                <fieldset>
                    <div class="form-field">
                        <input name="search" type="text" placeholder="search" size="50" autofocus />
                        <input type="submit" value="Search" />
                    </div>
                </fieldset>
                <div class="pageselector-results tiles">
                    <ul>
                        
                        <!-- pages collection -->
                        <li data-bind="id: id" tabindex="0" class="tile col5 hover" data-collection="pages"
                            data-event="clickItem, keypress: selectItemOnEnter">
                            <div class="media">
                                <img src="{{thumbUrl}}" />
                            </div>
                            <div class="caption">
                                <h3>{{title}}</h3>
                                <span>{{previewText}}</span>
                            </div>
                        </li>
                        <!-- -->
                        

                    </ul>
                </div>
            </form>
        </div>
    </div>
</section>